//now we need to create functions that will take form details and possibly work on them

export const UserLogin =({values, setFieldError, history, setSubmitting}) => {

}

export const UserSignin =({values, setFieldError, history, setSubmitting}) => {

}
export const UserLogout =() => {

}